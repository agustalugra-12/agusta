# Changelog

Semua perubahan penting pada proyek Pelangi PMS (HotelSync AI) dicatat di sini.
Format longgar mengikuti [Keep a Changelog](https://keepachangelog.com/).

## [Unreleased]

### Added
- Backend/Frontend: **Booking Request (Tahap 1 — Modul Reservasi & Priority Booking)** — lampu hijau user 2026-07-17. AI WhatsApp (`backend/routes/pesan_whatsapp.py`) sekarang mengumpulkan data booking lewat percakapan multi-turn natural (bukan sekali tanya-jawab): mendeteksi niat booking (`_klasifikasi_niat_booking`), menyimpan progres per nomor HP di collection baru `wa_booking_sessions` (basi otomatis setelah 2 jam), lalu setelah field wajib lengkap & tamu konfirmasi, memanggil `buat_booking_request()` (`backend/routes/booking_requests.py`, BUKAN endpoint HTTP publik) — AI tetap TIDAK PERNAH membuat booking sungguhan, konsisten dengan batasan yang sudah berlaku di bot Telegram. Collection baru `booking_requests` (status waiting_approval/waiting_payment/rejected). Halaman staf baru **Booking Request** (`frontend/src/pages/BookingRequests.jsx`, `/booking-requests`) — resepsionis/owner tinjau ketersediaan (termasuk cek manual ke PMS RedDoorz, di luar sistem), pilih kamar spesifik, lalu Terima (`POST /booking-requests/{id}/approve` — reuse penuh `create_reservation`+`tripay_create_transaction` yang sudah teruji, tidak ada jalur pembayaran paralel baru, link dikirim otomatis ke tamu via WA) atau Tolak (`POST /booking-requests/{id}/reject`, AI kirim alasan ke tamu). Booking Request yang menunggu persetujuan juga tampil sebagai alert langsung di Dashboard utama (bukan cuma halaman terpisah), dialog Terima/Tolak di-reuse dari `BookingRequests.jsx`. Diverifikasi live end-to-end: simulasi percakapan WA 4 giliran, approve → booking sungguhan + link Tripay sandbox asli berhasil, reject path, dan balasan AI utk pesan non-booking dipastikan tidak berubah sama sekali.
- Backend/Frontend: **Tahap 2 — Action Required RedDoorz + sinkron email + matikan booking Menginap instan** (2026-07-17, lanjutan Tahap 1 di atas). Keputusan bisnis user: `/book` publik sekarang HANYA melayani Day Use instan seperti biasa — tab Menginap tetap bisa dilihat (preview kamar/harga) tapi bagian pembayaran diganti CTA "Chat Admin via WhatsApp" (`PublicBook.jsx`), `public_create_booking` (`backend/routes/public.py`) menolak `tipe=menginap` sebagai guard tambahan. Booking Menginap dari Booking Request membawa `sync_status`: `waiting_reddoorz_input` begitu dibuat → `waiting_reddoorz_sync` setelah staf klik "Sudah Input ke RedDoorz" (endpoint baru `POST /bookings/{id}/reddoorz-input-selesai`, section "Action Required" baru di halaman Booking Request DAN Dashboard) → `synced` otomatis begitu AI Email Parser menerima email konfirmasi RedDoorz yang cocok (`_cocokkan_booking_pending_reddoorz` di `backend/routes/otomasi_email.py`, mencocokkan nama+tanggal+tipe kamar — mencegah booking duplikat, email OTA yang tidak cocok tetap membuat booking baru seperti biasa). `check_room_available` TIDAK berubah — slot tetap terkunci penuh dari overbooking selama proses ini. Yang berubah cuma tampilan: Kalender Ketersediaan (`kalender_bulanan`/`ketersediaan_hari`, `backend/routes/ketersediaan.py`) dan grid Dashboard mengecualikan booking `sync_status` `waiting_reddoorz_*` dari hitungan "terisi"/badge Booked — sengaja **TIDAK** disembunyikan dari Laporan keuangan (uang sudah diterima nyata lewat Tripay). Diverifikasi end-to-end lewat skrip standalone (approve → tandai input → email cocok → synced tanpa duplikat) DAN live di browser (Dashboard: alert Booking Request + Action Required + dialog Terima muncul & berfungsi, PublicBook.jsx: CTA WhatsApp Menginap tampil benar dengan ringkasan booking).
- Backend/Frontend: **Notifikasi Push PWA** — booking baru (online), pembayaran diterima (Tripay), komplain/maintenance baru, dan kamar perlu dibersihkan sekarang dikirim sebagai push notification lewat service worker, sampai ke browser staf/owner walau tab PMS tidak sedang dibuka. Backend: VAPID keypair (env `VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY`/`VAPID_CLAIM_EMAIL`), library `pywebpush`, collection baru `push_subscriptions` (per browser/device, terikat `user_id`+`role`), route baru `backend/routes/push.py` (`GET /push/vapid-public-key`, `GET /push/status`, `POST /push/subscribe`, `POST /push/unsubscribe`) + helper `send_push(title, body, url, role=, user_id=)` best-effort (gagal kirim ke 1 device tidak menghentikan alur bisnis utama, subscription expired/410 otomatis dibersihkan). Dipanggil dari `routes/public.py` (booking online baru), `routes/tripay.py` (webhook pembayaran lunas), `routes/issues.py` (komplain/maintenance baru), `routes/rooms.py` + `routes/checkins.py` (kamar jadi `perlu_dibersihkan`, broadcast ke role resepsionis). Frontend: service worker baru `public/sw.js` (listener `push`+`notificationclick`, sengaja tanpa asset caching), helper `src/lib/pushNotifications.js` (register/subscribe/unsubscribe/status), tombol "Aktifkan Notifikasi Push" di halaman Profil (`PushNotifLink`, pola sama seperti link Telegram). Icon PWA baru (`icon-192.png`/`icon-512.png`/`badge-96.png`, digenerate dari logo) ditambahkan ke `manifest.json`.
- Frontend: **Cetak struk ke printer thermal Bluetooth** — tombol "Cetak Bluetooth" baru berdampingan dengan "Cetak Struk" (browser print) yang sudah ada, di halaman Kasir dan Check-Out. Pakai Web Bluetooth API langsung dari browser (`src/lib/blePrinter.js`) — connect ke printer BLE generik (GATT service/characteristic "generic serial" umum dipakai printer thermal 58/80mm murah), kirim command ESC/POS mentah (init, teks, cut). Hanya jalan di Chrome Android/Desktop (origin HTTPS) — tombol otomatis tersembunyi di browser yang tidak dukung (mis. Safari/iOS), fallback tetap cetak browser biasa.
- Frontend: **Kirim bukti transaksi via WhatsApp (teks biasa, bukan PDF)** — halaman Check-Out: tombol WA existing diperkaya jadi struk lengkap (`buildCheckoutReceiptMessage` di `apiClient.js`, bukan cuma 1 baris ucapan terima kasih). Halaman Kasir (POS): field opsional "Nama Pembeli"/"No. HP" di keranjang + tombol baru "Kirim Bukti Transaksi WA" (`buildKasirReceiptMessage`) setelah transaksi sukses — kasir sebelumnya anonim/walk-in tanpa identitas tamu sama sekali.
- Frontend: halaman sukses booking publik (`/book/sukses`) — tombol "Unduh Voucher (PDF)" dihapus (permintaan user: jangan tawarkan PDF), tetap kirim detail booking lengkap (kamar, tanggal, status DP/Lunas, sisa tagihan) sebagai teks biasa lewat tombol WA yang sudah ada.
- Backend/Frontend: **Laporan Shift (per petugas)** — tab baru "Laporan Shift" di `/laporan`. Sistem ini tidak punya konsep clock-in/clock-out asli, jadi laporan dirangkum dari jejak petugas yang sudah tercatat di tiap modul (kasir, check-in/out, pengeluaran, housekeeping), dikelompokkan per tanggal+petugas. Endpoint baru `GET /reports/shift?from_date&to_date` (`backend/routes/reports.py`), tabel ringkasan per petugas + detail per hari, export CSV.

### Fixed
- Backend: **booking untuk tanggal mendatang salah ditolak "Kamar tidak tersedia"** — `create_reservation()` (`backend/reservation_service.py`, dipakai booking publik `/book` DAN Booking Request baru) memblokir berdasarkan status kamar SAAT INI, bukan ketersediaan di tanggal yang diminta, jadi tamu bisa ditolak salah cuma karena kamar itu kebetulan sedang dipakai tamu lain sekarang walau sudah kosong lagi jauh sebelum check-in. Bug pre-existing, ditemukan saat uji live alur approve Booking Request (2026-07-17) — status kamar saat ini sekarang hanya menghalangi booking yang mulai hari ini juga, tanggal mendatang divalidasi murni lewat `check_room_available`, konsisten dengan logika `public_availability`.
- Frontend: dialog Setujui Booking Request tertutup sebelum link pembayaran Tripay sempat ditampilkan/disalin staf — ditemukan saat uji live yang sama, sekarang dialog tetap terbuka menampilkan link sampai staf klik Selesai.
- Telegram Bot: dikonfirmasi ekstraksi pengeluaran teks bebas (owner bot) bekerja normal end-to-end (test langsung ke prod: "bensin 500rb" → tercatat Rp50.000 di `db.expenses`, muncul di halaman Pengeluaran dengan badge Telegram). Root cause laporan "belum masuk ke data pengeluaran" dari sisi **staff** ternyata staff belum pernah menghubungkan akun Telegram-nya (`telegram_chat_id` kosong) — bukan bug kode, staf perlu generate kode link dari halaman Profil lalu `/start <kode>` ke bot staff.
- Backend/Frontend: **"Coba Bayar Lagi" untuk booking yang otomatis dibatalkan karena pembayaran expired/gagal** — sebelumnya sengaja ditunda (ditandai prioritas rendah setelah dinilai ulang 2026-07-12 bahwa dampaknya cuma UX), sekarang dikerjakan atas persetujuan user. Endpoint baru `POST /public/bookings/{id}/retry-bayar` (`backend/routes/public.py`) membuka lagi booking `cancelled` yang dibatalkan OTOMATIS oleh webhook Tripay/Midtrans karena expired/gagal bayar — dibedakan dari pembatalan mandiri tamu, staf, atau auto-cancel modifikasi OTA (ketiganya tidak pernah menghasilkan `payment_status` `expired`/`failed`, jadi kombinasi status ini sudah cukup unik tanpa perlu field tambahan). Kamar di-cek ulang ketersediaannya (`check_room_available`) sebelum dibuka lagi — kalau sudah diambil tamu lain, ditolak dengan pesan jelas (bukan double-booking). Frontend `PublicBook.jsx`: tombol "Coba Bayar Lagi" muncul di halaman sukses tamu untuk booking berstatus gagal, membuka dialog pilih channel Tripay + DP50/Lunas (`RetryBayarDialog`) yang memanggil retry-bayar lalu langsung `create-transaction` dalam satu submit, redirect ke `checkout_url` baru. Diverifikasi lewat skrip standalone, 5 skenario (expired → sukses, dibatalkan mandiri/staf → ditolak, kamar sudah terisi booking lain → ditolak, status bukan cancelled → ditolak), data uji dihapus.
- Backend/Frontend: **dialog "Buat Tagihan Baru" (halaman Pembayaran) sekarang memanggil Tripay sungguhan**, bukan simulasi lagi. `backend/routes/tripay.py` (`tripay_create_transaction`) diperluas — sebelumnya hanya menerima booking berstatus `booking_pending` (khusus alur checkout tamu publik), sekarang juga menerima status `aktif` (booking dari Quick Book staf, dipakai untuk reservasi telepon/WA), dengan syarat `payment_status` belum `paid` (mencegah tagih ganda). `Pembayaran.jsx`: daftar booking di dialog diambil dari `GET /payments/bookings-status?status_bayar=belum_bayar` (data nyata) dan pilihan channel dari `GET /payments/tripay/channels` (live dari Tripay), hasilnya `checkout_url` Tripay asli yang bisa disalin/dibuka untuk dikirim ke tamu. Kredensial Tripay masih **sandbox** — begitu Tripay approve produksi, cukup ganti env var `TRIPAY_MERCHANT_CODE`/`TRIPAY_API_KEY`/`TRIPAY_PRIVATE_KEY`/`TRIPAY_IS_PRODUCTION` di server + restart service, tanpa perubahan kode (`TRIPAY_BASE_URL` sudah otomatis switch). Diverifikasi lewat skrip standalone terhadap sandbox Tripay live: booking `aktif` belum bayar → tagihan berhasil dibuat (channel BRIVA, nominal DP 50% benar); booking sudah lunas → ditolak; booking `cancelled` → ditolak. Data uji dihapus setelahnya.

### Added
- Backend/Frontend: **tamu publik (`/book`) sekarang bisa booking LEBIH DARI 1 kamar sekaligus dalam 1 transaksi pembayaran** — lanjutan permintaan user 2026-07-14 (setelah sisi staf). `PublicBookingCreate` (`backend/core.py`) tambah `room_ids` (opsional, `room_id` tunggal tetap didukung penuh). `public_create_booking` (`backend/routes/public.py`): kalau `room_ids` diisi, semua kamar dicek ketersediaannya dulu (all-or-nothing — tamu menunggu live di checkout), lalu 1 dokumen booking per kamar dengan harga DIHITUNG SENDIRI dari tarif masing-masing kamar (bukan dibagi rata — grup bisa campur Standard+Cottage), semua menandai `group_id` yang sama. **Pembayaran Tripay digabung jadi 1 transaksi**: `tripay_create_transaction` (`backend/routes/tripay.py`) mendeteksi `group_id` pada booking yang dikirim, lalu menagih TOTAL GABUNGAN semua kamar dalam grup itu (DP 50% atau lunas, dihitung dari jumlah `total`/`dp_min` seluruh kamar) dalam 1x checkout — `amount_due` tiap kamar diisi proporsional terhadap `total` masing-masing (grup harga campuran tetap adil). Webhook `tripay_callback` juga group-aware: 1 notifikasi pembayaran dari Tripay meng-update SEMUA kamar dalam grup sekaligus (status/payment_status/paid_at/kirim voucher per kamar), bukan cuma kamar pertama. `GET /public/bookings/{id}` menyertakan `group_bookings` (kamar lain 1 transaksi) kalau ada. Frontend `PublicBook.jsx`: tombol kamar di katalog jadi toggle multi-select (checklist biru), bar mengambang "Lanjutkan (N Kamar)" di step 1, ringkasan & halaman sukses menampilkan semua kamar + total gabungan. **Catatan**: pembatalan mandiri (`/batalkan`) dan coba-bayar-lagi (`/retry-bayar`) SENGAJA tetap per-kamar (tamu bisa membatalkan 1 dari beberapa kamar tanpa membatalkan semuanya) — beda dari grup OTA otomatis yang selalu satu kesatuan. Voucher/email konfirmasi juga masih dikirim terpisah per kamar (belum digabung jadi 1 email/PDF berisi semua kamar). Diverifikasi lewat skrip standalone terhadap DB live + sandbox Tripay sungguhan: booking 2 kamar sejenis (lunas, 1 webhook → keduanya lunas), booking 2 kamar beda tipe/harga (DP 50%, rasio proporsional benar, keduanya konsisten status DP), dan booking single-room lama (response tetap flat dict, tidak berubah) — semua lolos, data uji dihapus.
- Backend/Frontend: **staf sekarang bisa Quick Book/check-in LEBIH DARI 1 kamar sekaligus** dari Dashboard (mis. rombongan walk-in) — permintaan user 2026-07-14 ("booking engine dan dashboard belum bisa melakukan bookingan lebih dari 1 kamar"). `BookingCreate`/`CheckinCreate` (`backend/core.py`) tambah field `room_ids` (opsional, `room_id` lama tetap didukung penuh — response tetap 1 dict datar kalau cuma 1 kamar, jadi `{"group_id", "bookings"/"checkins": [...]}` kalau lebih dari 1, supaya tidak ada test/consumer lama yang bergantung ke bentuk lama sampai jadi rusak). `POST /bookings` & `POST /checkins` (`backend/routes/bookings.py`, `backend/routes/checkins.py`): kalau `room_ids` diisi, semua kamar dicek ketersediaannya dulu SEBELUM satupun dibuat (all-or-nothing untuk grup — beda dari grup OTA otomatis yang partial-fulfillment-nya wajar karena async, di sini staf menunggu live jadi tidak boleh ada kamar setengah jalan), lalu dibuat 1 dokumen booking/checkin per kamar dengan `tipe`/`tarif_override`/`dengan_sarapan` yang sama, semua menandai `group_id` yang sama. Frontend `Dashboard.jsx`: tombol "Pilih Banyak Kamar" di atas grid kamar mengaktifkan mode multi-select (klik kamar kosong untuk toggle pilih, badge centang biru + border putus-putus), lalu "Lanjut (N)" membuka dialog Quick Book yang sama tapi beroperasi atas seluruh grup (judul & ringkasan harga menyesuaikan, harga yang diisi berlaku per kamar + baris "Total N Kamar" tambahan). Diverifikasi lewat skrip standalone terhadap DB live (create_booking & create_checkin, single vs grup 3 kamar, harga & room_id per kamar benar) — semua lolos, data uji dihapus.

### Fixed
- Backend/Frontend: **kartu "Kosong" di Dashboard tidak berkurang meski sudah ada booking untuk hari ini, dan tidak ada tombol check-in untuk booking OTA/walk-in** — laporan user (2026-07-14). Root cause tunggal untuk keduanya: booking berstatus `aktif` (semua booking OTA RedDoorz, dan booking menginap walk-in yang dibuat staf lewat Quick Book) tidak pernah mengubah `room.status` sampai tamu benar-benar di-check-in, DAN tombol "Check-in Tamu" (`Dashboard.jsx`) sebelumnya cuma muncul untuk status `booking_paid` (booking online tamu) — status `aktif` sama sekali tidak dapat aksi check-in. Diperbaiki: (1) `POST /bookings/{id}/checkin` (`backend/routes/bookings.py`) sekarang menerima status `aktif` selain `booking_paid`, dan **sekaligus memperbaiki bug lama** — sebelumnya SELALU set `room.status="day_use"` tak peduli tipe booking-nya, sekarang kalau `tipe=="menginap"` set `room.status="menginap"` tanpa membuat dokumen `checkins` (field durasi_jam/overtime di situ memang semantik Day Use), konsisten dengan alur Quick Book staf. Tombol "Check-in Tamu" muncul juga untuk status `aktif`. (2) `GET /reports/summary` (dipakai kartu ringkasan Dashboard) sekarang menghitung kamar `kosong` yang SUDAH ada booking (aktif/booking_pending/booking_paid) yang menempati hari ini sebagai kategori baru **"Dipesan (Belum Tiba)"**, bukan lagi ikut terhitung "Kosong" — kartu statistik baru ditambahkan di Dashboard. Diverifikasi terhadap data live: ketemu 5 kamar nyata yang sebelumnya salah terhitung "Kosong" padahal sudah dibooking hari ini; skrip standalone memverifikasi siklus penuh (booking aktif → masuk "Dipesan (Belum Tiba)" → check-in → masuk "Menginap"/"Day Use" sesuai tipe, room status benar).

### Removed
- Backend: **kode Midtrans dihapus sepenuhnya** — keputusan bisnis user 2026-07-14 ("pakai Tripay saja, Midtrans tidak dipakai lagi"). Dihapus karena sudah jadi dead code (tidak ada satupun pemanggilan dari frontend sejak Tripay dipasang 2026-07-13, lihat [[project-tripay-payment-gateway]]): `GET /payments/midtrans/config`, `POST /payments/midtrans/create-snap-token`, `POST /payments/midtrans/notification` (webhook), `GET /payments/midtrans/status/{order_id}` (`backend/routes/payments.py`); `import midtransclient`, `snap_client`, `MIDTRANS_SERVER_KEY`/`MIDTRANS_CLIENT_KEY`/`MIDTRANS_IS_PRODUCTION`, model `CreateSnapTokenBody` (`backend/core.py`); dependency `midtransclient` (`requirements.txt`); env var Midtrans di `pms-backend.service` (server live); test file `test_midtrans.py` & `TestMidtrans` di `test_iter25_refactor_smoke.py`. **Data historis TIDAK dihapus** — dokumen `payment_log` lama dari transaksi Midtrans nyata tetap ada dan tetap tampil di halaman Pembayaran (endpoint listing/shared seperti `list_payment_log`, `update_payment_status_manual`, `status_bayar_booking()`, `guess_booking_kode_from_order_id()` semuanya dipertahankan karena dipakai bersama Tripay). Field widget Dashboard yang namanya menyesatkan (`midtrans_total_count`/`midtrans_total_sum` — sebenarnya sudah menjumlah riwayat Tripay+Midtrans sejak awal, bukan Midtrans doang) diganti nama jadi `payment_total_count`/`payment_total_sum` (`backend/routes/reports.py`), widget Dashboard "Total Midtrans" → "Total Transaksi Bayar". Diverifikasi: backend restart bersih, endpoint live tetap sehat, tidak ada regresi pada dashboard.

### Fixed
- Backend: **"Pendapatan Hari Ini"/"Pendapatan Bulan Ini"/"Laba Bersih Bulan" di Dashboard utama tidak menghitung booking online/OTA/WhatsApp yang sudah lunas** — laporan user (2026-07-14) bahwa data Dashboard "tidak sinkron": jumlah booking naik (mencerminkan fix multi-kamar OTA di atas, yang membuat lebih banyak dokumen booking terpisah per kamar) tapi kartu pendapatan/laba di sebelahnya tidak ikut naik. Root cause: `/reports/daily` & `/reports/rooms` (`backend/routes/reports.py`) sudah lebih dulu diperbaiki (2026-07-13) untuk menggabung revenue booking `source` ota/online/whatsapp yang `payment_status=paid` (dibucket per `paid_at`) — tapi `/reports/summary`, endpoint yang justru dipakai Dashboard utama, terlewat waktu itu dan masih murni dari `checkins`+`kasir`+`services` (cuma walk-in). Diperbaiki: `report_summary` sekarang ikut menjumlah booking OTA/online/WhatsApp lunas (query sama persis dengan `/reports/daily`) ke `pendapatan_kamar_hari_ini`/`pendapatan_hari_ini`/`pendapatan_bulan_ini`/`laba_bersih_bulan_ini`. Diverifikasi terhadap data live: booking OTA/online/WA lunas bulan berjalan sebesar Rp6.140.352 (31 booking) yang sebelumnya sama sekali tidak masuk hitungan, sekarang ikut terhitung.
- **Pemulihan data (2026-07-14)**: setelah fix multi-kamar di bawah, dicek ulang SEMUA 23 booking `source=ota` yang ada terhadap isi raw email Gmail aslinya (regex "Jumlah Kamar" di body) — ketemu 4 booking yang masih kena bug lama (dibuat sebelum fix, mencatat cuma 1 kamar padahal email aslinya minta lebih): `BKO-20260712201928-195E` (Nita Ratnasari, 2 kamar Standard), `BKO-20260712201928-B2E9` & `BKO-20260712201928-F1ED` (Bayu Lukito & yopi yopi, masing-masing 2 kamar "Superior Room without AC"→Standard), `BKO-20260714062830-D0E2` (wayan shanti, 3 kamar Standard — booking yang dilaporkan user). Ditemukan juga sekalian: mapping tipe kamar RedDoorz "Superior Room without AC" → PMS Standard yang disebut CHANGELOG 2026-07-12 pernah ditambahkan ternyata hilang dari `room_mappings` (root cause tidak diketahui, ditambahkan kembali). Ke-4 booking lama dibatalkan dan digantikan booking baru sesuai jumlah kamar & harga per-kamar yang benar (total harga tetap sama seperti tercatat/email, kamar penggantinya dicek dulu bebas bentrok). Diverifikasi: scan ulang seluruh 23 booking OTA tidak ada lagi yang kurang tercatat.
- Backend/Frontend: **satu email OTA yang memesan LEBIH DARI SATU kamar sekaligus (mis. RedDoorz field "Jumlah Kamar : 3") hanya pernah menghasilkan 1 reservasi PMS untuk 1 kamar** — ditemukan dari contoh email nyata user (RedDoorz kirim "Jumlah Kamar : 3", "Jumlah Tamu : 2*3" untuk 1 nomor reservasi). Sebelumnya `EmailExtractedData` (`backend/core.py`) tidak punya konsep jumlah kamar sama sekali dan `buat_reservasi_otomatis` (`backend/routes/otomasi_email.py`) selalu berhenti di kamar kosong PERTAMA yang ditemukan — akibatnya 2 dari 3 kamar yang sebenarnya sudah dipesan tamu OTA tidak tercatat apa pun di PMS, tampak kosong di Dashboard/grid ketersediaan, berisiko double-booking oleh staf front office. Diperbaiki: field baru `jumlah_kamar` (`EmailExtractedData`) diekstrak AI dari "Jumlah Kamar" di email (`PARSE_SYSTEM_PROMPT` juga diinstruksikan memisahkan format "Jumlah Tamu: A*B" jadi tamu-per-kamar `A`, bukan total); `buat_reservasi_otomatis` sekarang membuat SATU booking per kamar (loop sampai `jumlah_kamar` kamar berbeda ditemukan kosong, harga total email dibagi rata per kamar), semua menandai `ota_reservation_no` yang sama. Kalau kamar kosong lebih sedikit dari yang diminta, tetap dibuat sebanyak yang aman (bukan didiamkan semua) + `Manual_Required` untuk sisanya. `batalkan_reservasi_otomatis` & `proses_modifikasi_otomatis` (dipakai email modifikasi/pembatalan OTA susulan, dicocokkan lewat `ota_reservation_no` yang sama) diperluas dari `find_one` jadi loop atas SEMUA kamar dalam grup — reschedule grup ditolak seluruhnya (bukan sebagian) kalau salah satu kamar bentrok, supaya keputusan tetap konsisten untuk satu reservasi tamu. `EmailLog` tambah field `reservation_ids` (list, selain `reservation_id` tunggal yang dipertahankan untuk kompatibilitas). Frontend `OtomasiEmail.jsx`: form "Proses Manual" & dialog detail log menampilkan `jumlah_kamar`. Diverifikasi lewat 5 skenario standalone terhadap DB live (buat 3 kamar sekaligus, reschedule grup, batalkan grup, partial fulfillment 15 diminta/10 tersedia, deteksi pembatalan terselubung tanggal-sama-persis) — semua lolos, data uji dihapus.
- Backend/Frontend: **staf tidak bisa lihat status bayar DP vs Lunas di halaman internal (Dashboard & Reservasi)** — perbaikan sebelumnya (`status_bayar_booking()`) baru dipasang di halaman tamu publik, staf masih melihat `payment_status` mentah (cuma "paid"/belum, tidak beda DP dari lunas). `GET /bookings` (list) dan `GET /bookings/{id}` (`backend/routes/bookings.py`) sekarang menyertakan `status_bayar`/`jumlah_dibayar`/`sisa_tagihan` di tiap item (`status_bayar_booking()` juga diperluas mengembalikan `jumlah_dibayar`, bukan cuma `sisa_tagihan`). Frontend: helper bersama `statusBayarOf()`/`STATUS_BAYAR_LABEL`/`STATUS_BAYAR_BADGE_CLASS` (`apiClient.js`) dipakai `DaftarReservasi.jsx` (kolom & badge baru "Status Bayar" di tabel + panel detail, rincian sudah dibayar/sisa) dan `Dashboard.jsx` (panel detail booking). Label lifecycle `booking_paid` diganti dari "Lunas" jadi "Terkonfirmasi" di `DaftarReservasi.jsx` supaya tidak tabrakan makna dengan badge status bayar yang baru (booking bisa "Terkonfirmasi" tapi masih DP). Pesan konfirmasi WA (`buildBookingConfirmationMessage`) juga dirombak untuk bisa bilang "DP diterima" (sebelumnya cuma biner Lunas/Menunggu).
  - **Bug data ditemukan saat verifikasi**: 21 booking RedDoorz (OTA) yang sudah `payment_status=paid` di DB live ternyata tidak pernah diisi `amount_due` sejak awal (root cause: `buat_reservasi_otomatis`, `backend/routes/otomasi_email.py`, sebelum perbaikan ini set `payment_status="paid"` tapi tidak pernah set `amount_due`) — akibatnya semua tampil salah sebagai "DP — Belum Lunas" dengan sisa tagihan sebesar total penuh, padahal sesuai kebijakan bisnis yang sudah disetujui (booking RedDoorz selalu dianggap lunas) seharusnya "Lunas". Diperbaiki di 2 lapis: (1) `buat_reservasi_otomatis` sekarang set `amount_due=total` untuk booking OTA baru; (2) 21 booking lama di DB live di-backfill `amount_due=total` lewat skrip standalone read/write langsung ke Mongo (bukan lewat `server.py`, sesuai [[backend-verify-no-fresh-instance]]) — nilai lama dibackup ke JSON sebelum ditulis. Diverifikasi sebelum & sesudah: 21 booking pindah dari "dp" (salah) ke "lunas" (benar), 18 booking `belum_bayar` & 1 booking lunas lain tidak terpengaruh.
- Backend/Frontend: **status pembayaran DP tamu publik salah ditampilkan sebagai "PAID" (lunas), padahal baru DP** — laporan user 2026-07-14: `/book/sukses` & voucher PDF menampilkan `payment_status` mentah booking, yang webhook Midtrans/Tripay sama-sama set jadi `"paid"` begitu ada settlement APAPUN payment_option-nya (DP 50% atau lunas), jadi tamu yang baru bayar DP tetap melihat "PAID" tanpa info sisa yang harus dilunasi di lokasi. Ditambah helper bersama `status_bayar_booking()` (`backend/core.py`, dipakai ulang menggantikan `_status_bayar` lokal di `routes/payments.py`) yang derive status 3-keadaan (belum_bayar/dp/lunas) + `sisa_tagihan` dari `total` vs `amount_due`. Dipasang di `GET /public/bookings/{id}` (dipakai `/book/sukses`), voucher PDF & email konfirmasi (`backend/email_service.py`) — sekarang tampil "DP — BELUM LUNAS" + nominal sisa yang harus dibayar di lokasi, bukan "PAID" begitu saja. Diverifikasi lewat skrip motor standalone read-only terhadap data live (booking lunas → `lunas`/sisa 0; booking DP masih pending → `belum_bayar` sampai webhook benar-benar settle, sesuai ekspektasi).
- Backend: **webhook Tripay/Midtrans bisa kehilangan update booking + gagal kirim email voucher kalau `payment_log` untuk `order_id` itu tidak ketemu** (root cause salah satu laporan "email tidak terkirim") — ditemukan lewat data live: ada callback Tripay `PAID` sungguhan (`reference: DEV-...`) untuk booking `BKO-20260708230009-3575` yang tidak match `payment_log` manapun, jadi masuk cabang fallback lama yang cuma insert log yatim (`booking_id: null`) tanpa pernah update booking/kirim voucher. Ditambah `guess_booking_kode_from_order_id()` (`backend/core.py`) — order_id/merchant_ref selalu berpola `{kode}-{HHMMSS}{3hex}`, jadi kalau `payment_log` aslinya tidak ketemu, booking ditebak dari pola itu supaya tetap ter-update & voucher tetap terkirim, bukan diam-diam jadi entri yatim. Dipasang di `routes/tripay.py` (`tripay_callback`) & `routes/payments.py` (`midtrans_notification`). Diverifikasi: skrip standalone konfirmasi kode booking berhasil ditebak dari order_id orphan asli tsb dan booking-nya ketemu.
- Backend: **`payment_log.transaction_status` dari Tripay pakai vocab uppercase sendiri (PAID/UNPAID/EXPIRED/FAILED/REFUND)**, beda dari Midtrans (lowercase settlement/pending/expire/deny/cancel/refund) di field yang sama — bikin filter status & `VALID_PAYMENT_STATUSES` di endpoint ubah-status-manual staf tidak konsisten untuk transaksi asal Tripay. Ditambah `TRIPAY_STATUS_MAP` (`routes/tripay.py`) — status Tripay dinormalisasi ke vocab Midtrans sebelum ditulis ke `payment_log`, termasuk status awal saat `create-transaction` (`"pending"`, bukan `"UNPAID"`).
- Backend/Frontend: halaman staf **Pembayaran (`Pembayaran.jsx`) tabel transaksi utama masih data tiruan (`MOCK_TRANSACTIONS`)** meski riwayat & ubah-status per transaksi sudah nyata — ditambah endpoint `GET /payments/log` (aggregation join `nama_tamu` dari booking terkait, filter status, search) dan frontend disambungkan ke situ. Dialog "Buat Tagihan Baru" (staf buat tagihan untuk booking telepon/WA) masih sengaja simulasi — belum dipanggilkan ke `POST /payments/tripay/create-transaction` sungguhan karena endpoint itu butuh pilihan channel Tripay spesifik yang belum ada UI-nya.
- Backend: **email modifikasi OTA susulan diam-diam diabaikan sebagai "duplikat", padahal seharusnya membatalkan booking** — dedup guard di `proses_modifikasi_otomatis` (`backend/routes/otomasi_email.py`) memperlakukan SEMUA email modifikasi lanjutan sebagai duplikat murni begitu status booking sudah `direschedule`, sehingga tidak pernah sampai ke pengecekan "tanggal baru sama persis dengan yang tersimpan = pembatalan terselubung". Ditemukan lewat laporan user: reservasi no. OTA 444263135238733 di-reschedule otomatis ke 2026-07-20/21, lalu RedDoorz kirim email modifikasi kedua dengan tanggal SAMA PERSIS — seharusnya auto-cancel, tapi malah ditandai "Sudah_Diproses" tanpa aksi, booking tetap `booking_pending` dan kamar tetap dianggap terisi/menginap. Diperbaiki: dedup guard sekarang hanya berlaku untuk status `menunggu_review`; status `direschedule` tetap diproses lagi dan dibandingkan terhadap jadwal booking yang SEKARANG tersimpan (bukan jadwal asli). Booking yang sudah terlanjur tersangkut (BKO-20260712201928-83B1) dikoreksi manual (dibatalkan via `batalkan_reservasi_otomatis`, kamar dilepas) setelah izin user.
- Frontend: **grid kamar Dashboard (halaman utama staf) juga masih menganggap hari check-out sebagai "booked"** — bug ketiga dengan akar yang sama (setelah kalender Ketersediaan & `public/availability`), kali ini di sisi FRONTEND: `bookingsOnDate` (`Dashboard.jsx`) memfilter booking pakai overlap timestamp mentah (`start <= dayEnd && end >= dayStart`) buat nentuin ribbon booking per kamar di tanggal yang dipilih staf — hari check-out booking menginap selalu ikut tampil "Booked Menginap" walau tamu sudah checkout. Ini kemungkinan besar yang terus dilihat user sebagai "booking Widya masih bermasalah" meski 2 fix backend sebelumnya sudah benar, karena Dashboard adalah halaman yang paling sering dibuka staf. Diperbaiki dengan logika yang sama (rentang tanggal `[checkin_date, checkout_date)`, exclusive checkout, kecuali day-use). Diverifikasi lewat unit test JS standalone (4 skenario: sebelum checkin, hari checkin, hari checkout multi-booking, setelah checkout semua) — semua lolos.
- Backend: **`GET /public/availability` (pencarian kamar booking publik) juga masih menganggap hari check-out sebagai bentrok** — bug yang sama dengan kalender Ketersediaan (lihat entri di bawah) tapi di endpoint terpisah (`routes/public.py`) yang belum ikut diperbaiki sebelumnya. Ditemukan lewat laporan user (booking checkin 12/checkout 13 masih tidak muncul tersedia di tanggal 13) — dikonfirmasi langsung lewat `curl` ke API live sebelum fix (room hilang dari daftar tersedia), dan setelah fix (room muncul lagi). Diperbaiki dengan helper `_booking_date_range` (rentang tanggal `[checkin, checkout)` exclusive checkout, kecuali day-use) — kandidat booking overlap diambil dari Mongo (query timestamp lama tetap dipakai sebagai pre-filter kasar, aman karena superset), lalu difilter presisi di Python per rentang tanggal. Anti-overbooking sungguhan saat booking benar-benar dibuat (`check_room_available`) TIDAK diubah — tetap pakai overlap timestamp presis seperti sebelumnya, cuma pencarian/tampilan ketersediaan yang diperhalus.
- Backend: **kalender Ketersediaan (`kalender-bulanan`, `ketersediaan/hari`) menghitung hari check-out sebagai terisi** — logika lama pakai overlap TIMESTAMP mentah (`b_end >= day_start`), yang membuat hari check-out booking menginap SELALU ikut terhitung terisi walau tamu sudah checkout paginya (mis. checkin tanggal 20/checkout tanggal 21 tampil terisi di 20 **dan** 21, padahal cuma 1 malam yang seharusnya terisi di tanggal 20 saja). Ditemukan lewat laporan user pada alur reservasi otomatis dari email OTA (baik saat dibuat maupun setelah reschedule modifikasi), tapi bug-nya sistemik — memengaruhi semua booking menginap, bukan cuma yang dibuat otomasi email. Diperbaiki dengan helper `_occupies_date` (`backend/routes/ketersediaan.py`) yang menghitung occupancy berbasis rentang TANGGAL `[checkin_date, checkout_date)` (checkout day dikecualikan), kecuali booking day-use yang checkin/checkout di hari yang sama (tetap terhitung terisi hari itu). Diverifikasi dengan 11 skenario standalone (1 malam, multi-malam, day-use, termasuk 2 kasus persis yang dilaporkan user) — semua lolos.
- Frontend: **`/book/sukses/` (tanpa ID booking) nyasar ke halaman login admin, bukan halaman sukses/voucher** — route React sebelumnya cuma mendaftarkan `/book/sukses/:bookingId` (wajib ID), jadi URL tanpa ID jatuh ke catch-all dan redirect ke dashboard (yang ter-proteksi login). Kemungkinan pemicu nyata: "Finish Redirect URL" Midtrans di dashboard mereka bersifat statis per akun untuk sebagian metode pembayaran, tidak selalu bisa disisipi ID transaksi. Diperbaiki (`App.js`, `PublicBook.jsx`): booking id disimpan ke `localStorage` saat checkout, `SuccessView` fallback ke situ kalau URL tidak menyertakan ID (lalu membetulkan URL), dan tampilkan kartu "Detail Booking Tidak Ditemukan" (+ tombol WhatsApp CS) kalau ID tetap tidak ada — bukan lagi nyasar ke login admin. Diverifikasi dengan Playwright atas build produksi + booking uji nyata (3 skenario), data uji dihapus.
- Backend: **root cause ketemu kenapa booking RedDoorz tidak pernah tersinkron** — query pencarian Gmail (`OTA_QUERY`, `backend/routes/otomasi_email.py`) dari awal cuma mencari domain agoda.com/traveloka.com/booking.com, TIDAK PERNAH mencari reddoorz.com sama sekali, jadi email RedDoorz nyata di inbox tidak pernah kebaca walau auto-fetch sudah jalan tiap 1 menit. Diperbaiki sesuai instruksi user (2026-07-12): PMS ini sekarang **hanya fokus** membaca email dari `bookings-indonesia@reddoorz.com` di `agus.lugra@gmail.com` (`SENDER_DOMAIN_SUMBER`/`OTA_QUERY` diganti total, bukan lagi generik Agoda/Traveloka/Booking.com). Dropdown sumber OTA di `OtomasiEmail.jsx`/`PemetaanTipeKamar.jsx` disesuaikan.
- Backend: fix `batalkan_reservasi_otomatis` (`otomasi_email.py`) tidak update `status`/`alasan` log ke `Parsed_Success` saat berhasil membatalkan — sebelumnya log tetap kelihatan `Manual_Required` dengan alasan lama walau `aksi` sudah `reservasi_dibatalkan`, membingungkan staf yang melihat Log Email Masuk.
- **Pemulihan data (2026-07-12)**: setelah fix di atas, 19 email RedDoorz nyata yang sempat nyangkut sejak sebelum fix (belum terpetakan tipe kamarnya) diproses ulang manual sekali (skrip standalone, bukan endpoint) — 2 pemetaan tipe kamar baru ditambahkan (`Superior Room without AC` & `Standard Room without A/C` → PMS Standard), menghasilkan 18 reservasi nyata masuk ke PMS (status `booking_pending`, belum lunas sesuai data RedDoorz). 1 reservasi (no. OTA 444265135238215, "yaya yaya") terbukti dari urutan Gmail message ID sudah dimodifikasi/dibatalkan RedDoorz setelah dibuat — dibatalkan lagi di PMS supaya konsisten dengan RedDoorz.

### Changed
- Frontend: **panel "Alternatif: Transfer Manual ke Rekening" dihapus** dari halaman sukses booking tamu (`PublicBook.jsx` `SuccessView`, permintaan user 2026-07-12) — state/fetch `banks` (`GET /public/bank-accounts`) yang jadi tidak terpakai ikut dibersihkan dari halaman ini. Endpoint backend-nya tidak dihapus (masih dipakai test suite lama, sengaja dibiarkan ada kalau nanti dibutuhkan lagi di tempat lain).
- Backend: **voucher PDF (`generate_voucher_pdf`, `backend/email_service.py`) sekarang menampilkan "Sudah Dibayar (DP)" dan "Sisa Bayar"** (permintaan user 2026-07-12) — dihitung dari `total` vs `amount_due` booking (field yang sama dipakai `_status_bayar`/`sisa_tagihan` di `routes/payments.py`, nominal kumulatif yang confirmed terkumpul, bukan cuma DP awal kalau sudah ada pelunasan susulan lewat collect-balance). Berlaku untuk semua voucher (unduh dari halaman sukses tamu maupun kirim otomatis via email). Diverifikasi generate PDF untuk 3 skenario (DP saja, lunas penuh, belum bayar sama sekali) + render visual PDF-ke-gambar, hasil dicek manual.
- Frontend: katalog booking publik (`PublicBook.jsx`, step pilih kamar) untuk mode **Menginap** sekarang menampilkan **4 pilihan harga sekaligus** (Standard Tanpa/Dengan Sarapan, Cottage Tanpa/Dengan Sarapan) sebagai tombol pilihan langsung di tiap kartu tipe kamar — sebelumnya breakfast cuma bisa dipilih lewat toggle tersembunyi di step 2 setelah kamar dipilih, harga yang tampil di katalog cuma satu (tanpa sarapan). Pilihan sarapan yang diklik di step 1 sekarang terbawa ke ringkasan step 2 (toggle di step 2 tetap ada untuk ubah pikiran, tetap sinkron).
- Backend/Frontend: **koreksi harga kamar (2026-07-12)** — Day Use dan Menginap ternyata harus punya tarif dasar terpisah, bukan satu field `rooms.tarif` yang dipakai bersama (kesalahan pada perubahan harga sebelumnya di hari yang sama, yang tanpa sengaja menaikkan harga Day Use juga). Sekarang: `rooms.tarif` = Day Use (Standard Rp120.000, Cottage Rp140.000, flat per 6 jam, dikembalikan ke nilai semula) dan field baru `rooms.tarif_menginap` = Menginap per malam tanpa sarapan (Standard Rp150.000, Cottage Rp200.000; +Rp25.000/malam dengan sarapan jadi Rp175.000/Rp225.000, tidak berubah dari sebelumnya). Migrasi DB live + seed `server.py` + model `RoomCreate`/`RoomUpdate` (`backend/core.py`) + form Kelola Kamar (`frontend/src/pages/Rooms.jsx`, sekarang ada 2 kolom tarif terpisah). Perhitungan harga menginap (`backend/routes/public.py`, `backend/routes/bookings.py`, fallback harga OTA di `backend/routes/otomasi_email.py`) dan tampilan (`PublicBook.jsx` katalog & ringkasan harga, `Dashboard.jsx` Quick Book — toggle Day Use/Menginap sekarang ganti default harga sesuai tarif yang relevan) disesuaikan ke field baru ini. Diverifikasi ulang end-to-end (Day Use 120rb, Menginap tanpa sarapan 150rb/malam) lewat booking uji sungguhan, data uji dihapus setelahnya.
- Backend: Otomasi Email & Pemesanan sekarang **fully automatic, tanpa staf klik "Cek Email Baru"** — background loop baru `background_gmail_fetch_loop` (`backend/routes/otomasi_email.py`, di-jadwalkan di `server.py` seperti `background_sync_loop`) mengecek Gmail tiap 1 menit (`GMAIL_AUTO_FETCH_INTERVAL_SECONDS`) selama Gmail terhubung, gagal (token expired/API down) di-log tapi tidak menghentikan loop. Tombol "Cek Email Baru" di UI tetap ada untuk cek manual instan.
- Backend: AI Email Parser sekarang mengklasifikasi `jenis` notifikasi OTA — **baru** (reservasi baru, alur lama tidak berubah) vs **modifikasi/pembatalan** (keputusan bisnis user 2026-07-12: modifikasi dianggap sama seperti pembatalan). Untuk modifikasi/pembatalan, reservasi PMS yang cocok dicari lewat field baru `ota_reservation_no` (disimpan di booking saat dibuat otomatis) dan langsung **dibatalkan otomatis** (fungsi baru `batalkan_reservasi_otomatis`) — tidak menebak detail baru. Kalau reservasi lama tidak ketemu, `email_logs` jadi `Manual_Required` dengan alasan jelas (tidak dipaksakan). `EmailLog` (`backend/core.py`) tambah field `jenis`/`reservation_id`/`aksi` untuk dokumentasi & tampilan staf. Frontend `OtomasiEmail.jsx`: dialog detail log jenis-aware (tidak lagi render field kosong untuk email modifikasi/pembatalan yang datanya memang minimal). Diverifikasi end-to-end lewat skrip standalone (reservasi baru → dibatalkan via no. OTA, dan path "tidak ketemu" → Manual_Required), data uji dihapus setelahnya.
- Backend/Frontend: **modifikasi OTA — dua revisi susulan sama hari (2026-07-12, keputusan bisnis user)**. Revisi 1: email "modifikasi" tidak lagi disamakan dengan pembatalan (terlalu berisiko — bisa berarti reschedule) — booking terkait ditandai `modifikasi_status="menunggu_review"`, staf konfirmasi lewat tab baru "Modifikasi OTA" (`OtomasiEmail.jsx`) dan 2 endpoint baru: `POST /otomasi-email/modifikasi/{id}/reschedule` & `.../batalkan`. Revisi 2 (menggantikan sebagian Revisi 1): AI Email Parser sekarang coba ekstrak check-in/check-out BARU dari isi email modifikasi (kalau eksplisit disebutkan, tidak dikarang — `PARSE_SYSTEM_PROMPT`); `proses_modifikasi_otomatis` (`backend/routes/otomasi_email.py`) membandingkan dengan jadwal lama — sama persis (checkin & checkout) → auto-cancel; salah satu beda → auto-reschedule (tetap dicek anti-overbooking via `check_room_available`). Tab "Modifikasi OTA" sekarang jadi fallback murni, cuma dipakai kalau AI tidak menemukan tanggal baru yang jelas atau jadwal baru bentrok kamar lain. Index Mongo baru `bookings.modifikasi_status` (sparse). Diverifikasi lewat skrip standalone, 6 skenario (tanggal sama, checkin beda, checkout beda saja, data kosong, bentrok kamar, email susulan duplikat) — semua lolos, data uji dibersihkan.
- Backend/Frontend: harga kamar disesuaikan dengan tarif asli homestay dan ditambah opsi Sarapan Pagi untuk booking Menginap. `rooms.tarif` (harga dasar tanpa sarapan) diubah dari data lama Standard Rp120rb/Cottage Rp140rb menjadi **Standard Rp150rb / Cottage Rp200rb** (dimigrasikan langsung di DB live + seed di `server.py` untuk instalasi baru) — harga ini otomatis konsisten di seluruh aplikasi (grid Dashboard, Quick Book, katalog booking publik) karena semuanya baca dari field yang sama. Field baru `dengan_sarapan` (bool) di `BookingCreate`/`PublicBookingCreate` (`backend/core.py`) + konstanta `BREAKFAST_PRICE = 25000`/malam (`Standard+sarapan Rp175rb, Cottage+sarapan Rp225rb` — selisih konsisten Rp25rb) — dihitung server-side di `public_create_booking` (`backend/routes/public.py`) dan `create_booking` staf (`backend/routes/bookings.py`, hanya berlaku kalau staf tidak pakai `tarif_override`). Frontend `PublicBook.jsx`: toggle "Sarapan Pagi" muncul khusus saat pilih Menginap, masuk ke breakdown harga & payload booking, ditampilkan di halaman sukses/voucher (`backend/email_service.py` ikut menampilkan baris "Sarapan Pagi" di PDF voucher kalau dipilih). Diverifikasi end-to-end lewat booking uji sungguhan (dihapus dari DB setelah dikonfirmasi benar).
- Backend/Frontend: booking publik (`/book`) sekarang bisa **Menginap**, tidak lagi cuma Day Use — sebelumnya `public_create_booking` (`backend/routes/public.py`) hardcode selalu +6 jam dari check-in walau backend generic (`create_reservation`) sudah tipe-aware sejak lama. `PublicBookingCreate` (`backend/core.py`) tambah `tipe`/`tanggal_checkout`; check-out menginap fixed jam 12:00 WIB, harga dihitung per malam (tarif kamar × malam, extra bed juga per malam — beda dari Day Use yang tetap flat). `GET /api/public/availability` tambah param opsional `checkout` supaya anti-overbooking mengecek seluruh rentang malam, bukan cuma 1 hari. Frontend `PublicBook.jsx`: toggle Day Use/Menginap + date picker tanggal check-out (auto-hitung jumlah malam), summary & disclaimer pembatalan (H-3 vs H-1) jadi tipe-aware. Pesan konfirmasi WA (`buildBookingConfirmationMessage` di `frontend/src/lib/apiClient.js`) sekarang menyertakan tanggal check-out & jumlah malam untuk booking menginap (sebelumnya tidak pernah menyebut checkout sama sekali, tipe apapun).
- Frontend: halaman booking publik (`PublicBook.jsx`) sekarang menampilkan alamat Pelangi Homestay (Jl. Kebun Raya Bedugul, Desa Candikuning, Kec. Baturiti, Tabanan - Bali) dan tombol chat WhatsApp CS (0895356644644) di footer, plus copy hero/footer ditulis ulang dengan nuansa sejuk & alami khas Bedugul.
- Backend/Frontend: klik kamar kosong di grid Dashboard sekarang membuka dialog "Quick Book" — staf pilih tipe (Day Use/Menginap) dan **harga custom** langsung di tempat, lalu tercatat seketika (Day Use → `POST /api/checkins`, Menginap → `POST /api/bookings` + `PUT /api/rooms/{id}/status` menandai kamar terisi). Field baru `tarif_override` (opsional, harus > 0) di `CheckinCreate`/`BookingCreate` (`backend/core.py`), dipakai `backend/routes/checkins.py` & `backend/routes/bookings.py` menggantikan tarif dasar kamar kalau diisi. Menggantikan alur lama yang memaksa staf ke halaman terpisah `/checkin/:roomId` (Day Use, harga tetap ikut kamar) atau form "Booking Baru" terpisah. Halaman `frontend/src/pages/Bookings.jsx` (`/bookings`) dan `frontend/src/pages/CheckIn.jsx` (`/checkin/:roomId`) **dihapus total** — sudah digantikan sepenuhnya oleh dialog Quick Book ini. Item sidebar "Booking" dihapus.
- Frontend: fix warna overlay booking pada grid kamar Dashboard — booking tipe **menginap** sekarang biru (`#3B82F6`, konsisten dengan warna status "Menginap"), booking **day_use** tetap coklat (`#92400E`). Sebelumnya semua booking yang akan datang (apapun tipenya) selalu ditampilkan coklat.
- Backend: `GET /api/rooms` (`backend/routes/rooms.py`) sekarang urut murni numerik berdasarkan nomor kamar (1→18), bukan dikelompokkan per tipe kamar dulu — nomor kamar Cottage (1-8) dan Standard (9-18) tidak berurutan per tipe sehingga pengelompokan lama membuat tampilan grid kamar terlihat acak. Nomor/nama kamar tidak diubah.
- Frontend: halaman "Tamu" (`frontend/src/pages/Tamu.jsx`, `/tamu`) digabung ke halaman "Reservasi" (`frontend/src/pages/DaftarReservasi.jsx`, `/reservasi`) sebagai tab kedua — permintaan user supaya sidebar ringkas dan kedua fitur (riwayat reservasi + riwayat kunjungan/nomor WA tamu) saling melengkapi dalam satu halaman. Tab "Reservasi" sekaligus disambungkan ke data nyata (`GET /bookings` dengan `search`/`status`/`date`, yang ternyata sudah didukung backend sejak lama tapi tak pernah dipakai frontend) menggantikan `MOCK_RESERVATIONS`. Item sidebar "Tamu" dihapus, route `/tamu` dihapus.

### Added
- Backend: `GET /api/ketersediaan/hari?tanggal=YYYY-MM-DD` — breakdown ketersediaan per tipe kamar untuk satu tanggal tertentu (logika overlap booking sama dengan `kalender-bulanan`, dikelompokkan per tipe) — `backend/routes/ketersediaan.py`. Dipakai dialog detail hari di Kalender Ketersediaan.
- Frontend: halaman "Ketersediaan Kamar" (Dasbor Ketersediaan, Fase 1) disambungkan ke data nyata — sebelumnya masih 100% data tiruan (`MOCK_ROOM_TYPES`) meski backend-nya (`ketersediaan.py`) sudah lama ada, tidak pernah dipanggil frontend. Ringkasan/Status Tipe Kamar/Notifikasi sekarang dari `GET /api/ketersediaan/live` (polling 10 detik). Kalender Ketersediaan diubah dari tampilan 1 bulan penuh (30 hari) jadi 1 minggu (7 hari) dengan navigasi per minggu, data dari `GET /api/ketersediaan/kalender-bulanan` — `frontend/src/pages/Ketersediaan.jsx`.

- Backend: Integrasi Pembayaran (Fase 3, halaman "Pembayaran") — `GET /api/payments/log/by-booking/{booking_kode}` (riwayat percobaan pembayaran per reservasi), `PUT /api/payments/log/{log_id}/status` (koreksi status transaksi manual oleh staf, ikut update status booking + kirim voucher otomatis kalau jadi lunas), `GET /api/payments/bookings-status` (daftar reservasi + status bayar terderivasi belum_bayar/dp/lunas, filter-able) — `backend/routes/payments.py`, `backend/core.py` (`PaymentStatusUpdateBody`). Frontend `Pembayaran.jsx`: panel "Riwayat Pembayaran" dan tombol "Ubah Status" disambungkan ke endpoint nyata (tabel daftar transaksi utama masih data tiruan, menunggu endpoint list payment_log).
- Backend: skema `AuditLog` didokumentasikan di `backend/core.py` (Log Aktivitas/Audit Trail, Fase 3) — koleksi `audit_log` & service `log_activity()` sudah dipakai luas sejak awal proyek di modul stok kamar, reservasi, pengguna, dll; `GET /api/audit-log` sudah tersambung penuh ke frontend `Audit.jsx` (`/audit`) sejak batch sebelumnya. Diverifikasi ulang end-to-end 2026-07-11, tanpa kode baru selain skema.
- Backend/Frontend: pendaftaran akun mandiri (Fase 3, halaman "Autentikasi & Pengelolaan Akun") — `POST /api/auth/register`, akun baru default role resepsionis & status `pending` sampai diaktifkan Owner lewat halaman Pengguna — `backend/routes/auth.py`, `backend/core.py` (`RegisterIn`). Frontend halaman "Daftar Akun" (`/register`, `frontend/src/pages/Register.jsx`) + tautan dari Login, badge "menunggu aktivasi" di tabel Pengguna.
- Backend: endpoint Laporan & Analitik (Fase 3) — pendapatan harian, performa saluran (OTA/Website/WhatsApp), tren okupansi — dari data booking multi-saluran sungguhan (`bookings`, `payment_status=paid`) + `checkins` (walk-in) untuk okupansi. Beda dari `/reports/*` (P&L operasional walk-in, Fase 1) — `backend/routes/laporan_analitik.py`. Index baru `bookings.(payment_status, paid_at)` dan `bookings.source`. Frontend `LaporanAnalitik.jsx` disambungkan (bukan mock lagi).
- Backend: Manajemen Harga/Rates (Fase 3, halaman "Kalender Harga") — collection `rates` (override harga per tanggal per tipe kamar, tarif dasar tetap di `rooms.tarif`), `GET /api/rates/kalender`, `GET /api/rates/tipe-kamar`, `POST /api/rates/update-massal` (terapkan harga ke rentang tanggal, satu atau semua tipe kamar) — `backend/routes/rates.py`, `backend/core.py` (`RateOverride`, `RateBulkUpdateBody`). Update harga mendorong event sinkronisasi ke bot WhatsApp lewat `push_sync_event` yang sama dipakai sinkronisasi ketersediaan. Frontend `KalenderHarga.jsx` disambungkan (bukan mock lagi).
- Frontend/Backend: halaman "Profil Saya" (`/profil`, `frontend/src/pages/Profil.jsx`) — lihat info akun + ubah nama/password sendiri, diakses dari klik nama di sidebar (`Layout.jsx`). Backend `PUT /api/auth/me` — self-service update, beda dari `PUT /users/{id}` (owner-only, bisa ubah siapa saja tanpa password lama); wajib verifikasi password lama untuk ganti password — `backend/routes/auth.py`, `backend/core.py` (`MeUpdate`).
- Backend: pengiriman voucher email sungguhan via Brevo — `backend/email_service.py` (baru), dipicu otomatis begitu booking lunas (webhook Midtrans di `backend/routes/payments.py`, atau konfirmasi transfer manual staf di `backend/routes/bookings.py`), plus endpoint kirim ulang manual `POST /api/pengiriman-voucher/kirim-ulang/{booking_id}` — `backend/routes/public.py`. Setiap percobaan kirim (sukses/gagal) dicatat ke `email_send_log`. Kredensial `BREVO_API_KEY`/`BREVO_FROM_EMAIL`/`BREVO_FROM_NAME` dipasang di `pms-backend.service` — `backend/core.py`.
- Backend: generator PDF voucher sungguhan (reportlab) — `GET /api/public/bookings/{id}/voucher.pdf`, dipakai tombol "Unduh Voucher" di halaman konfirmasi tamu (ganti dari `window.print()`) — `backend/routes/public.py`, `backend/requirements.txt`.
- Backend: skema `EmailSendLog` + endpoint `GET /api/pengiriman-voucher/logs` — `backend/core.py`, `backend/routes/public.py`. Frontend `PengirimanVoucherOtomatis.jsx` disambungkan.
- Backend: dashboard Sinkronisasi Data PMS -> bot WhatsApp (status aliran data, perbandingan bot/PMS, referensi reservasi, alert) + `push_sync_event` (dorong notifikasi ke webhook bot tiap perubahan stok, dengan retry) — `backend/routes/sinkronisasi_data_pms.py`, `backend/core.py`. Frontend `SinkronisasiDataPMS.jsx` disambungkan.
- Backend: endpoint `POST /api/public/bookings/{id}/batalkan` — pembatalan mandiri tamu SEKARANG OTOMATIS PENUH (bukan lagi "ajukan permintaan"), sesuai keputusan bisnis yang dikonfirmasi user 2026-07-11. Update status, lepas kamar, hitung biaya H-3/H-1 (sama dengan kalkulasi frontend), catat `refund_amount` (refund uang tetap manual oleh staf), kirim notifikasi WA best-effort — `backend/routes/public.py`. Frontend `PublicBook.jsx` disambungkan (dialog batalkan tidak lagi cuma toast, benar-benar memanggil endpoint).
- Backend: `webhook_config` CRUD + uji koneksi sungguhan (HTTP call ke `webhook_url` staf) — `backend/routes/konfigurasi_webhook.py`.
- Backend: webhook receiver publik `/api/webhook/whatsapp/incoming` — balasan otomatis via AI (OpenAI, konteks ketersediaan kamar real-time), kirim via provider WhatsApp yang dikonfigurasi staf — `backend/routes/pesan_whatsapp.py`, collection baru `wa_conversations`/`wa_connection_log`/`wa_sync_settings`.
- Backend: endpoint stats, log percakapan, log pengiriman per-arah, ringkasan kegagalan, log koneksi, alert, kirim-ulang — semua dari data sungguhan.
- Frontend: `KonfigurasiWebhook.jsx`, `PesanWhatsAppOtomatis.jsx`, `PemantauanStatusWA.jsx` disambungkan penuh ke endpoint di atas.
- Backend: field `extra_bed_qty` di booking publik (`PublicBookingCreate`, `reservation_service.create_reservation`) — harga +Rp 50.000 flat per bed (maks 2), dihitung server-side. Frontend `/book` sekarang punya selector Extra Bed sungguhan (`ExtraBedSelector` dipakai ulang dari halaman pratinjau) yang mengubah harga & tersimpan ke reservasi nyata — `backend/core.py`, `backend/reservation_service.py`, `backend/routes/public.py`, `frontend/src/pages/PublicBook.jsx`, `frontend/src/pages/PermintaanKhususExtraBed.jsx`.
- Backend: endpoint GET `/api/jenis-reservasi` — daftar jenis layanan (Day Use/Menginap) + aturan bisnisnya — `backend/routes/jenis_layanan.py`.
- Backend: endpoint GET `/api/rekomendasi-checkin` — rekomendasi jam check-in Day Use dari data booking Menginap sungguhan + jeda bersih-bersih, filter anti double-booking — `backend/routes/jenis_layanan.py`. Frontend `RekomendasiCheckinDayUse.jsx` disambungkan (bukan data tiruan lagi).
- Backend: endpoint status/paksa-sinkron/riwayat-stok/pengaturan untuk Sinkronisasi Ketersediaan + service penjadwalan otomatis (background asyncio loop) — `backend/routes/sinkronisasi_ketersediaan.py`, collection baru `sync_channels`/`sync_settings`, dipakai lagi `availability_logs` yang sudah ada.
- Frontend: `SinkronisasiKetersediaan.jsx` disambungkan penuh ke endpoint nyata di atas — tidak ada lagi data tiruan.
- Konfigurasi: aktivasi AI Email Parser — `OPENAI_API_KEY` dipasang di `pms-backend.service`.
- Backend: service pengambilan email Gmail (`POST /api/otomasi-email/gmail/fetch`) dengan refresh token otomatis — `backend/routes/otomasi_email.py`.
- Backend: AI Email Parser sungguhan pakai OpenAI (gpt-4o-mini) untuk ekstrak data reservasi dari isi email OTA — `backend/routes/otomasi_email.py`.
- Backend: Reservation Automation — reservasi otomatis dibuat di Pelangi PMS begitu email berhasil di-parse AI (anti double-booking: fallback ke Manual_Required kalau tipe kamar belum dipetakan atau kamar penuh) — `backend/routes/otomasi_email.py` (`buat_reservasi_otomatis`), pakai `reservation_service.create_reservation` yang sudah ada.
- Backend: endpoint GET `/api/otomasi-email/logs` (+ filter `status`), CRUD `/api/otomasi-email/mapping-rules`, POST `/api/otomasi-email/logs/{id}/proses-manual`.
- Frontend: `OtomasiEmail.jsx` disambungkan penuh ke endpoint nyata (Koneksi Gmail, Log Email Masuk, Aturan Pemetaan AI, Proses Manual) — tidak ada lagi data tiruan.
- Backend: skema `EmailLog`/`EmailExtractedData` (collection `email_logs`) — `backend/core.py`.
- Backend: model `RoomMappingCreate`/`RoomMappingUpdate` + endpoint CRUD `/api/mappings`, GET `/api/pms-room-types`, POST `/api/pms-room-types/sync`, GET `/api/unmapped-ota-rooms` — `backend/routes/pemetaan_tipe_kamar.py`, collection baru `room_mappings`.
- Frontend: `PemetaanTipeKamar.jsx` disambungkan ke endpoint nyata di atas (bukan data tiruan lagi).
- Konfigurasi: aktivasi Gmail OAuth di `pms-backend.service` (GOOGLE_CLIENT_ID/SECRET, GOOGLE_OAUTH_REDIRECT_URI, FRONTEND_URL) — service direstart, siap dipakai tombol "Hubungkan Gmail".
- Frontend: aturan jam check-in Day Use per hari (Minggu mulai 12:00, Senin-Sabtu mulai 10:00), validasi input jam — `frontend/src/pages/JenisReservasi.jsx`.
- Frontend: halaman baru "Paket Kamar" (`/paket-kamar`) — komponen `PaketKamarSelector` (dengan/tanpa breakfast per tipe kamar), data tiruan — `frontend/src/pages/PaketKamar.jsx`.
- Frontend: halaman baru "Rekomendasi Check-in" (`/rekomendasi-checkin`) — logika AI menyarankan jam check-in Day Use dari jam check-out Menginap malam sebelumnya + jeda bersih-bersih, alternatif kamar lain, skenario penuh, data tiruan — `frontend/src/pages/RekomendasiCheckinDayUse.jsx`.
- Frontend: notifikasi status kirim voucher ke email di halaman konfirmasi tamu — `frontend/src/pages/PublicBook.jsx`.
- Backend: endpoint koneksi Gmail OAuth (`/api/otomasi-email/gmail/connect`, `/callback`, `/status`, `/disconnect`) — `backend/routes/otomasi_email.py`, koleksi Mongo baru `integrations`.
- Frontend: dialog detail log email (data hasil ekstraksi AI / alasan gagal parsing) di tab "Log Email Masuk" halaman Otomasi Email & Pemesanan — `frontend/src/pages/OtomasiEmail.jsx`.
- Frontend: halaman "Otomasi Email & Pemesanan" (shell + tab navigasi, koneksi Gmail mock, log email mock) — `frontend/src/pages/OtomasiEmail.jsx`, route `/otomasi-email`.
- Frontend: tab "Aturan Pemetaan AI" — tabel aturan ekstraksi (sumber OTA, field, pola/kata kunci) + tambah/ubah/hapus/toggle aktif, data tiruan — `frontend/src/pages/OtomasiEmail.jsx`.
- Frontend: panel "Uji Aturan dengan Contoh Email" — jalankan regex aturan aktif terhadap contoh isi email yang ditempel staff (fungsional, bukan hasil acak) — `frontend/src/pages/OtomasiEmail.jsx`.
- Frontend: tab "Proses Manual" — daftar email berstatus Manual_Required/Failed + form isi data reservasi manual yang mengubah status email jadi Parsed_Success, data bersama (shared state) dengan tab Log Email Masuk — `frontend/src/pages/OtomasiEmail.jsx`.
- Frontend: halaman baru "Sinkronisasi Ketersediaan" — tab Status Sinkronisasi (status per saluran: Pelangi PMS, Website, Email OTA, WhatsApp Bot + tombol Paksa Sinkronisasi), data tiruan — `frontend/src/pages/SinkronisasiKetersediaan.jsx`, route `/sinkronisasi-ketersediaan`.
- Frontend: indikator "Live" (titik berdenyut + jam berjalan sejak cek terakhir, polling berkala 10 detik) di tab Status Sinkronisasi — `frontend/src/pages/SinkronisasiKetersediaan.jsx`.
- Frontend: tab "Riwayat Perubahan Stok" — tabel log pergerakan stok per kamar (waktu, perubahan +/-, alasan, sumber saluran), data tiruan — `frontend/src/pages/SinkronisasiKetersediaan.jsx`.
- Frontend: filter rentang tanggal + tipe kamar untuk tabel Riwayat Perubahan Stok — `frontend/src/pages/SinkronisasiKetersediaan.jsx`.
- Frontend: tab "Pengaturan" — pilih frekuensi sinkronisasi + atur prioritas saluran saat data bentrok (Pelangi PMS terkunci di posisi 1), data tiruan — `frontend/src/pages/SinkronisasiKetersediaan.jsx`.
- Frontend: halaman baru "Konfigurasi Webhook" — form endpoint & kredensial penyedia WhatsApp (Fonnte/Wablas/Qontak/custom), API key tersamar dengan toggle lihat, data tiruan — `frontend/src/pages/KonfigurasiWebhook.jsx`, route `/konfigurasi-webhook`.
- Frontend: halaman baru "Pembayaran" — daftar transaksi Midtrans (cari/filter status, detail dialog), bentuk data mengikuti koleksi `payment_log` yang sudah nyata di backend, data tiruan — `frontend/src/pages/Pembayaran.jsx`, route `/pembayaran`. Tidak mengubah alur checkout tamu (`PublicBook.jsx`) yang sudah berfungsi sungguhan.
- Frontend: halaman baru "Pesan WhatsApp Otomatis" — tab Ringkasan (statistik pesan/reservasi via WA hari ini + aktivitas terbaru), data tiruan — `frontend/src/pages/PesanWhatsAppOtomatis.jsx`, route `/whatsapp-otomatis`. Tab Log Percakapan & Pemantauan Status masih placeholder.
- Frontend: fitur "Buat Tagihan Baru" di halaman Pembayaran — staf pilih booking belum lunas + metode bayar (DP50/Lunas), simulasi Snap menghasilkan link pembayaran tiruan (copy/buka link) — `frontend/src/pages/Pembayaran.jsx`. Melengkapi (bukan menggantikan) alur checkout tamu asli di `PublicBook.jsx`.
- Frontend: tab "Pengaturan" di halaman Pesan WhatsApp Otomatis — toggle data yang disinkron ke bot (ketersediaan/harga/status booking/reservasi baru) + frekuensi, plus tautan ke halaman Konfigurasi Webhook (tidak menduplikasi form kredensialnya) — `frontend/src/pages/PesanWhatsAppOtomatis.jsx`.
- Frontend: tab "Log Percakapan" di halaman Pesan WhatsApp Otomatis — riwayat pesan masuk & balasan AI per tamu, cari nama/nomor, status kirim, data tiruan — `frontend/src/pages/PesanWhatsAppOtomatis.jsx`.
- Frontend: tombol "Uji Koneksi" di halaman Konfigurasi Webhook — simulasi ping ke penyedia WhatsApp, tampilkan hasil (berhasil/gagal + waktu uji) — `frontend/src/pages/KonfigurasiWebhook.jsx`.
- Frontend: tombol "Lihat Pembayaran" di detail Daftar Reservasi — buka halaman Pembayaran dengan filter kode booking otomatis terisi (`?kode=...`) — `frontend/src/pages/DaftarReservasi.jsx`, `frontend/src/pages/Pembayaran.jsx`.
- Frontend: perhalus feedback form Konfigurasi Webhook — badge "perubahan belum disimpan", validasi inline per field saat gagal simpan, tombol "Batalkan Perubahan" — `frontend/src/pages/KonfigurasiWebhook.jsx`.
- Frontend: halaman baru "Pemetaan Tipe Kamar" — tabel korelasi nama tipe kamar OTA (Agoda/Traveloka/Booking.com) dengan tipe kamar Pelangi PMS, data tiruan — `frontend/src/pages/PemetaanTipeKamar.jsx`, route `/pemetaan-tipe-kamar`.
- Frontend: halaman baru "Sinkronisasi Data PMS" — dasbor status aliran data (ketersediaan/harga/status booking/reservasi baru) dari Pelangi PMS ke bot WhatsApp, data tiruan — `frontend/src/pages/SinkronisasiDataPMS.jsx`, route `/sinkronisasi-data-pms`. Beda dari halaman "Sinkronisasi Ketersediaan" (lintas saluran penjualan) dan tab Pengaturan di Pesan WhatsApp Otomatis (pengaturan, bukan monitoring).
- Frontend: tombol "Batalkan Pesanan" (self-service) di halaman detail reservasi tamu (`PublicBook.jsx`, live/nyata) — dialog menampilkan kebijakan & biaya pembatalan yang dihitung sungguhan (H-1, 10%, kebijakan sama seperti pesan konfirmasi WA), aksinya mengajukan permintaan (bukan pembatalan instan palsu) karena backend belum punya endpoint pembatalan mandiri.
- Frontend: filter cari/tipe kamar PMS/sumber OTA di halaman Pemetaan Tipe Kamar — `frontend/src/pages/PemetaanTipeKamar.jsx`.
- Frontend: indikator "Live" di halaman Sinkronisasi Data PMS — `frontend/src/pages/SinkronisasiDataPMS.jsx`.
- Frontend: timer mundur (real, benar-benar berjalan) sampai batas bebas biaya di dialog Batalkan Pesanan tamu — `frontend/src/pages/PublicBook.jsx`.
- Frontend: tabel "Log Peringatan Gangguan" di halaman Sinkronisasi Data PMS — `frontend/src/pages/SinkronisasiDataPMS.jsx`.
- Frontend: tombol ubah/hapus (dengan modal edit & konfirmasi hapus) di tiap baris Pemetaan Tipe Kamar — `frontend/src/pages/PemetaanTipeKamar.jsx`.
- Frontend: tombol "Tambah Pemetaan" di halaman Pemetaan Tipe Kamar (dialog form dipakai bersama untuk tambah & ubah) — `frontend/src/pages/PemetaanTipeKamar.jsx`.
- Frontend: panel "Ketersediaan Kamar: Bot vs PMS" di halaman Sinkronisasi Data PMS — bandingkan jumlah kamar tersedia yang dilihat bot vs data PMS sungguhan per tipe kamar, tandai jika tidak cocok (drift) — `frontend/src/pages/SinkronisasiDataPMS.jsx`.
- Frontend: panel "Referensi Reservasi PMS" di halaman Sinkronisasi Data PMS — daftar reservasi yang jadi rujukan data yang disinkron ke bot + tautan ke Daftar Reservasi — `frontend/src/pages/SinkronisasiDataPMS.jsx`.
- Frontend: tombol "Impor dari PMS" (simulasi loading + notifikasi) di halaman Pemetaan Tipe Kamar — `frontend/src/pages/PemetaanTipeKamar.jsx`.
- Frontend: panel "Tipe Kamar OTA Belum Dipetakan" di halaman Pemetaan Tipe Kamar — tombol "Petakan" per item membuka form tambah pemetaan pre-filled (nama OTA + sumber) — `frontend/src/pages/PemetaanTipeKamar.jsx`.
- Frontend: komponen `ExtraBedSelector` + halaman pratinjau "Permintaan Khusus: Extra Bed", data tiruan — `frontend/src/pages/PermintaanKhususExtraBed.jsx`, route `/extra-bed`. Belum disambungkan ke checkout tamu nyata (backend belum punya field/harga extra bed).
- Frontend: halaman baru "Pemantauan Status" — statistik pengiriman pesan WA, area peringatan gangguan, log pengiriman pesan, data tiruan — `frontend/src/pages/PemantauanStatusWA.jsx`, route `/pemantauan-status-wa`. Tab placeholder "Pemantauan Status" lama di halaman Pesan WhatsApp Otomatis diubah jadi tautan ke halaman baru ini (menghindari duplikasi).
- Frontend: dialog detail pesan + tombol "Kirim Ulang" (mock) untuk pesan berstatus Gagal di halaman Pemantauan Status — `frontend/src/pages/PemantauanStatusWA.jsx`.
- Frontend: form pemesanan demo (tipe kamar + malam + ExtraBedSelector) dengan total harga dinamis di halaman Permintaan Khusus Extra Bed — `frontend/src/pages/PermintaanKhususExtraBed.jsx`. Sengaja tetap demo (bukan form live) karena backend belum punya field/harga extra bed sungguhan.
- Frontend: panel "Ringkasan Kegagalan" (dikelompokkan per alasan) + "Log Perubahan Status Koneksi" (naik-turun webhook) di halaman Pemantauan Status — `frontend/src/pages/PemantauanStatusWA.jsx`.
- Frontend: info "Permintaan Khusus: Extra Bed" di dialog detail Daftar Reservasi (data tiruan, halaman ini memang sudah mock sepenuhnya) — `frontend/src/pages/DaftarReservasi.jsx`.
- Frontend: info extra bed (kondisional, `bk.extra_bed_qty > 0`) di halaman voucher/konfirmasi booking tamu — `frontend/src/pages/PublicBook.jsx`. Live tapi aman: field belum ada di data nyata sehingga tidak pernah tampil sampai backend benar-benar mendukungnya, tidak memengaruhi total harga.

- Frontend: komponen `TipeReservasiSelector` (Day Use/Menginap) + halaman pratinjau "Jenis Reservasi", gaya konsisten dengan form staf Bookings.jsx — `frontend/src/pages/JenisReservasi.jsx`, route `/jenis-reservasi`. Belum diintegrasikan ke form booking tamu (masih selalu day_use).
- Frontend: form demo "Reservasi Menginap" (tanggal check-in, jumlah malam, tipe kamar, jumlah tamu, ringkasan check-in/out + total dinamis) di halaman Jenis Reservasi, muncul saat tipe Menginap dipilih — `frontend/src/pages/JenisReservasi.jsx`.
- Frontend: form demo "Reservasi Day Use" (tanggal/jam check-in, tipe kamar, jumlah tamu, tarif flat + biaya layanan 3% seperti perhitungan nyata) di halaman Jenis Reservasi, muncul saat tipe Day Use dipilih — `frontend/src/pages/JenisReservasi.jsx`.
- Frontend: notifikasi ketentuan Day Use (durasi 6 jam standar + biaya overtime Rp20.000/jam, sama seperti `calc_tagihan` nyata di backend) di form demo Day Use — `frontend/src/pages/JenisReservasi.jsx`.
- Frontend: validasi input (tanggal tidak boleh lampau, jumlah tamu minimal 1) di kedua form demo Jenis Reservasi — `frontend/src/pages/JenisReservasi.jsx`.
- Frontend: halaman admin baru "Log Pengiriman Voucher" — riwayat pengiriman voucher/bukti booking otomatis (email/WhatsApp), cari/filter status, data tiruan — `frontend/src/pages/PengirimanVoucherOtomatis.jsx`, route `/pengiriman-voucher`.

### Fixed
- **`backend/core.py` (belum sempat dirilis)** — field `metode` milik `CollectBalanceBody` (dipakai `POST /bookings/{id}/collect-balance`) sempat ke-insert ke tengah class baru `PaymentStatusUpdateBody`, membuat `CollectBalanceBody` kehilangan field itu (akan `AttributeError` saat dipanggil). Dibetulkan sebelum sempat di-deploy.
- **Halaman voucher booking (nyata)** — tanggal/jam Check-Out tidak pernah ditampilkan padahal API sudah mengembalikan `jam_selesai`. Ditambahkan baris Check-Out di `PublicBook.jsx`.
- **Kebijakan pembatalan H-3/H-1 (nyata, bukan mock)** — sebelumnya pesan konfirmasi WA & dialog batalkan pesanan selalu bilang H-1 untuk semua booking. Sesuai klarifikasi bisnis: menginap = bebas biaya sampai H-3, day use = H-1. Diperbaiki di `apiClient.js` (buildBookingConfirmationMessage, dipakai Dashboard/Bookings/PublicBook) dan `PublicBook.jsx` (dialog Batalkan Pesanan + timer mundur, dibuat tipe-aware).
- **PublicBook.jsx (halaman checkout tamu, live/nyata)** — sebelumnya booking yang pembayarannya expired/gagal (status `cancelled`) tidak menampilkan penjelasan apa pun ke tamu di halaman hasil. Sekarang ditampilkan status "Booking Dibatalkan" + info bahwa kamar sudah dilepas kembali, tanpa tombol "bayar ulang" palsu (backend belum mendukung retry — lihat catatan di TODO.md). Polling status juga dihentikan begitu status final (paid/cancelled) supaya tidak polling selamanya.

### Notes
- Integrasi Gmail OAuth backend belum bisa dipakai nyata sampai `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `GOOGLE_OAUTH_REDIRECT_URI` dikonfigurasi di environment `pms-backend.service` (perlu dibuat dulu di Google Cloud Console) — lihat rincian di laporan task terkait.
- Frontend halaman Otomasi Email masih memakai data tiruan (mock) untuk semua tab yang sudah dibangun; belum disambungkan ke endpoint backend di atas.
